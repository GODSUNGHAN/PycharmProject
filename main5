def tower(n,fro,to,tmp) :
    if n > 1:
        tower(n-1,fro,tmp,to)
        print("Move from",fro,"to",to)
        tower(n-1,tmp,to,fro)
    else:
        print("Move from",fro,"to",to)


tower(3,"A","C","B")
